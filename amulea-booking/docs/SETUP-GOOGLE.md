# Google 連携の設定手順

Google カレンダーとスプレッドシートに、予約を自動で書き込めるようにします。

- **かかる時間**: 20〜30分ほど
- **費用**: 無料（この規模の利用では料金は発生しません）
- **必要なもの**: `amulea.163@gmail.com` にログインできる状態

> **先に読んでください**
> 途中で分からなくなっても、設定を間違えても、壊れることはありません。
> `MOCK_MODE=true` のあいだは、この設定なしでも予約システムは動きます。
> 落ち着いて1つずつ進めてください。

---

## 全体の流れ

```
手順1  Google Cloud にプロジェクトを作る
手順2  カレンダーとシートの機能をONにする
手順3  「サービスアカウント」という専用アカウントを作る
手順4  鍵ファイル（JSON）をダウンロードする
手順5  カレンダーを、そのアカウントに共有する
手順6  スプレッドシートを作り、同じく共有する
手順7  取得した値を .env.local に書く
```

「サービスアカウント」とは、**人ではなくプログラム専用の Google アカウント**
のことです。これを作って、自分のカレンダーを「共有」してあげることで、
予約システムがカレンダーを読み書きできるようになります。

---

## 手順1　Google Cloud にプロジェクトを作る

1. `amulea.163@gmail.com` で Google にログインします。
2. ブラウザで <https://console.cloud.google.com/> を開きます。
3. 初回は利用規約の同意画面が出ます。同意して進みます。
4. 画面いちばん上の、青いバーの左側にある
   **プロジェクトを選択** をクリックします。
5. 右上の **新しいプロジェクト** をクリックします。
6. プロジェクト名に `amulea-booking` と入力します。
7. **作成** をクリックします。
8. 数十秒で作成されます。画面上部が `amulea-booking` に
   変わっていることを確認してください。

> ⚠️ **ここで一度確認**
> 以降の作業は、必ず `amulea-booking` プロジェクトが
> 選ばれている状態で行ってください。
> 画面上部の表示が違っていたら、クリックして切り替えます。

---

## 手順2　カレンダーとシートの機能をONにする

Google では、使いたい機能を1つずつONにする必要があります。

### カレンダー

1. 上部の検索窓に `Google Calendar API` と入力します。
2. 検索結果の **Google Calendar API** をクリックします。
3. 青い **有効にする** ボタンをクリックします。
4. 「APIが有効です」と表示されればOKです。

### スプレッドシート

1. 同じく検索窓に `Google Sheets API` と入力します。
2. **Google Sheets API** をクリックします。
3. **有効にする** をクリックします。

---

## 手順3　サービスアカウントを作る

1. 左上の **≡（三本線）** →
   **APIとサービス** → **認証情報** を開きます。
2. 上部の **＋ 認証情報を作成** をクリックします。
3. 一覧から **サービス アカウント** を選びます。
4. 次のように入力します。

   | 項目 | 入力する内容 |
   | --- | --- |
   | サービス アカウント名 | `amulea-booking` |
   | サービス アカウント ID | 自動で入ります。そのままでOK |
   | 説明 | `予約システム用`（空欄でも可） |

5. **作成して続行** をクリックします。
6. 「ロールを選択」は **何も選ばずに** **続行** をクリックします。
   （カレンダーの権限は、あとで「共有」で渡すためです）
7. 最後の画面も **完了** をクリックします。

---

## 手順4　鍵ファイル（JSON）をダウンロードする

1. **認証情報** の画面に、作ったサービスアカウントが
   一覧で表示されています。
2. そのメールアドレス（`amulea-booking@amulea-booking-XXXXXX.iam.gserviceaccount.com`
   のような長いもの）をクリックします。
3. 上部のタブから **キー** を選びます。
4. **鍵を追加** → **新しい鍵を作成** をクリックします。
5. キーのタイプは **JSON** を選び、**作成** をクリックします。
6. `amulea-booking-xxxxx.json` というファイルが
   パソコンにダウンロードされます。

> 🔐 **このファイルの扱い**
> このファイルは**家の鍵と同じ**です。
> - 誰にも送らないでください（メール・LINE・チャットすべてNG）
> - Git（GitHub）には**絶対に**登録しないでください
> - 設定が終わったら、パソコンから削除して構いません

7. **サービスアカウントのメールアドレスを、メモ帳などにコピーしておいてください。**
   次の手順で使います。

---

## 手順5　カレンダーを共有する

作ったサービスアカウントに、カレンダーを見る・書く権限を渡します。

1. <https://calendar.google.com/> を開きます
   （`amulea.163@gmail.com` でログインした状態で）。
2. 画面左の「マイカレンダー」から、予約を入れたいカレンダーを探します。
   通常は **あなたのお名前** のカレンダーです。
3. そのカレンダー名にマウスを乗せ、右に出る **⋮（縦三点）** をクリック。
4. **設定と共有** を選びます。
5. 下へスクロールして **特定のユーザーまたはグループと共有する** を探します。
6. **＋ ユーザーやグループを追加** をクリックします。
7. 手順4でメモした**サービスアカウントのメールアドレス**を貼り付けます。
8. 権限は **予定の変更権限** を選びます。

   > ⚠️ 「閲覧権限」では予約を書き込めません。
   > 必ず **予定の変更権限** を選んでください。

9. **送信** をクリックします。

### カレンダーIDを確認する

同じ「設定と共有」の画面を下へスクロールすると
**カレンダーの統合** という項目があります。
そこにある **カレンダー ID** をメモしてください。

ご自身のメインカレンダーであれば、
`amulea.163@gmail.com` がそのままカレンダーIDです。

---

## 手順6　スプレッドシートを作って共有する

1. <https://sheets.google.com/> を開きます。
2. **空白のスプレッドシート** をクリックして新規作成します。
3. 左上のファイル名を `Amulea予約データ` に変更します。
4. 右上の **共有** ボタンをクリックします。
5. 手順4でメモした**サービスアカウントのメールアドレス**を貼り付けます。
6. 権限は **編集者** を選びます。
7. 「通知を送信」のチェックを**外して**、**共有** をクリックします。

### スプレッドシートIDを確認する

ブラウザのURLを見てください。

```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz1234567890/edit
                                      └──────────── ここがID ────────────┘
```

`/d/` と `/edit` のあいだの長い文字列がIDです。これをメモします。

> 💡 シートのタブや見出し行は、**予約システムが自動で作ります**。
> 何も準備しなくて大丈夫です。

---

## 手順7　.env.local に書き込む

プロジェクトのフォルダにある `.env.example` をコピーして
`.env.local` という名前のファイルを作ります。

```bash
cp .env.example .env.local
```

`.env.local` をテキストエディタで開き、次の4つを埋めます。

### ① GOOGLE_CLIENT_EMAIL

手順4でメモしたメールアドレスです。

```
GOOGLE_CLIENT_EMAIL=amulea-booking@amulea-booking-123456.iam.gserviceaccount.com
```

### ② GOOGLE_PRIVATE_KEY

ダウンロードしたJSONファイルをテキストエディタで開きます。
`"private_key":` という行を探してください。

```json
"private_key": "-----BEGIN PRIVATE KEY-----\nMIIEvQIBADAN...（とても長い）...\n-----END PRIVATE KEY-----\n",
```

**`"` から `"` までを、ダブルクォートごと**コピーして貼り付けます。

```
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADAN...\n-----END PRIVATE KEY-----\n"
```

> 💡 **よくある質問**
> 「`\n` という文字が入っているけど大丈夫？」→ **大丈夫です。**
> これは改行を表す記号で、プログラムが自動的に本物の改行へ戻します。
> 手で改行に直さないでください。

### ③ GOOGLE_CALENDAR_ID

手順5でメモしたカレンダーIDです。

```
GOOGLE_CALENDAR_ID=amulea.163@gmail.com
```

### ④ GOOGLE_SPREADSHEET_ID

手順6でメモしたスプレッドシートIDです。

```
GOOGLE_SPREADSHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz1234567890
```

---

## 手順8　連携をONにして動作確認

`.env.local` の先頭にある `MOCK_MODE` を `false` にします。

```
MOCK_MODE=false
```

サーバーを起動し直します。

```bash
npm run dev
```

### 確認すること

1. ブラウザで <http://localhost:3000/admin> を開きます。
2. 管理画面にログインします。
3. 上部に「Google カレンダー連携が未設定です」という
   黄色い帯が **出ていなければ成功**です。
4. **手動で予約を登録** から、テストの予約を1件入れてみます。
5. Google カレンダーを開き、
   `Amulea予約｜○○` という予定ができていれば成功です。
6. スプレッドシートを開き、`reservations` というタブに
   1行追加されていれば成功です。
7. 確認できたら、そのテスト予約はキャンセルしてください
   （カレンダーからも自動で消えます）。

---

## うまくいかないとき

| 症状 | 原因と対処 |
| --- | --- |
| 「Google 認証に失敗しました」 | `GOOGLE_PRIVATE_KEY` の貼り付けミス。ダブルクォートごと貼り直してください |
| 「Google API エラー（events.insert / HTTP 404）」 | `GOOGLE_CALENDAR_ID` が違います。手順5で確認し直してください |
| 「Google API エラー（events.insert / HTTP 403）」 | カレンダーの共有権限が「閲覧」になっています。**予定の変更権限**に変更してください |
| 「Google API エラー（values.append / HTTP 403）」 | スプレッドシートの共有が「閲覧者」になっています。**編集者**に変更してください |
| カレンダーに何も出ない | `MOCK_MODE=false` になっているか確認し、サーバーを再起動してください |

エラーメッセージは管理画面には出ません。
サーバーを起動しているターミナルに表示されます。

---

## 覚えておきたい便利な使い方

設定が終わると、次のことが**Googleカレンダーを操作するだけ**でできます。

| やりたいこと | カレンダーでの操作 |
| --- | --- |
| その時間を予約不可にする | 普通に予定を入れる（例: 15:00-17:00「私用」） |
| その日を丸ごと休業にする | **終日**の予定を入れる（例:「休業」） |
| 予定はあるが予約は受けたい | 予定の「予定あり」を **「予定なし」** に変更する |

管理画面を開かなくても、いつものカレンダーで休業日を管理できます。
